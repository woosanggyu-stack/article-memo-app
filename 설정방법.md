# 설정 방법 — 구글시트 연동 + Streamlit Cloud 배포

이 앱은 데이터를 로컬 파일이 아니라 **구글시트**(텍스트 데이터)와 **구글드라이브**(이미지)에 저장합니다.
그래야 GitHub + Streamlit Community Cloud에 올려서 노트북·스마트폰 등 여러 기기에서 같은 메모를 볼 수 있습니다.

## 1. 구글 클라우드 프로젝트 만들고 API 켜기
1. https://console.cloud.google.com 접속 → 상단에서 새 프로젝트 만들기 (이름 예: `article-memo`)
2. 왼쪽 메뉴 "API 및 서비스" → "라이브러리"에서 아래 두 개를 검색해 각각 **사용 설정**:
   - `Google Sheets API`
   - `Google Drive API`

## 2. 서비스계정 만들고 키 받기
1. "API 및 서비스" → "사용자 인증 정보" → "사용자 인증 정보 만들기" → "서비스 계정"
2. 이름 아무거나 입력(예: `article-memo-bot`) 후 완료 (역할 부여는 건너뛰어도 됨)
3. 생성된 서비스계정 클릭 → "키" 탭 → "키 추가" → "새 키 만들기" → **JSON** 선택 → 다운로드
   - 다운로드된 JSON 파일 안의 `client_email` 값을 기억해두세요 (예: `xxx@article-memo.iam.gserviceaccount.com`). 다음 단계에서 이 이메일을 구글시트/드라이브에 "공유"해야 합니다.
   - **이 JSON 파일은 절대 GitHub에 올리지 마세요.** 비밀번호와 같습니다.

## 3. 구글시트 준비
1. https://sheets.google.com 에서 새 시트 생성 (이름 예: `기사메모 데이터`)
2. 오른쪽 위 "공유" 버튼 → 2단계에서 확인한 서비스계정 이메일을 추가하고 **편집자** 권한 부여
3. 주소창의 URL에서 시트 ID를 복사: `https://docs.google.com/spreadsheets/d/여기부분/edit` 의 `여기부분`

## 4. 구글드라이브 폴더 준비 (이미지 저장용)
1. https://drive.google.com 에서 새 폴더 생성 (이름 예: `기사메모 이미지`)
2. 폴더 우클릭 → 공유 → 서비스계정 이메일 추가, **편집자** 권한 부여
3. 폴더를 열었을 때 주소창 URL 끝부분이 폴더 ID: `https://drive.google.com/drive/folders/여기부분`

## 5. 로컬에서 실행할 secrets 설정
1. `.streamlit/secrets.toml.example` 파일을 복사해서 같은 폴더에 `secrets.toml`로 저장
2. 아래 값을 채워넣기:
   - `sheet_id` → 3단계에서 복사한 시트 ID
   - `drive_folder_id` → 4단계에서 복사한 폴더 ID
   - `[gcp_service_account]` 아래 항목들 → 2단계에서 받은 JSON 파일을 열어서 같은 이름의 값들을 그대로 옮겨 적기 (`private_key`는 줄바꿈이 `\n`으로 되어 있는 그대로 큰따옴표 안에 붙여넣으면 됨)
3. 저장 후 실행해서 확인:
```bash
py -m streamlit run app.py
```
사이드바에 오류 없이 "프로젝트가 없습니다" 화면이 뜨면 연결 성공입니다.

## 6. GitHub에 올리기
`secrets.toml`은 `.gitignore`에 이미 등록되어 있어 자동으로 제외됩니다. 아래처럼 커밋해도 안전합니다.
```bash
cd C:/Temp/memo_note/article_memo_app
git init
git add .
git commit -m "기사 메모 앱 초기 업로드"
```
GitHub에서 새 저장소를 만든 뒤 안내되는 명령어로 push하세요. **push 후 GitHub 저장소 페이지에서 `.streamlit/secrets.toml` 파일이 보이지 않는지 반드시 확인**하세요.

## 7. Streamlit Community Cloud 배포
1. https://share.streamlit.io 접속 → 구글/GitHub 계정으로 로그인
2. "New app" → 방금 만든 저장소, 브랜치, `app.py` 경로 지정 후 "Deploy"
3. 배포 화면 오른쪽 "Settings" (또는 "Advanced settings") → **Secrets** 탭에 로컬 `secrets.toml` 내용을 그대로 붙여넣기 → Save (앱이 자동 재시작됨)
4. **접근 제한 설정** (취재 메모라 민감할 수 있으므로 권장):
   - 앱 관리 화면 → "Settings" → "Sharing" → "Who can view this app"를 **Only specific people**로 변경
   - 본인 구글 계정 이메일을 추가

## 8. 참고
- 이미지는 구글드라이브에 비공개로 저장되고, 앱이 서비스계정 권한으로 대신 읽어와 화면에 보여줍니다. 드라이브 링크를 직접 공유해도 남이 볼 수 없습니다.
- 메모/프로젝트를 삭제해도 드라이브에 올라간 이미지 파일 자체는 자동으로 지워지지 않습니다. 필요하면 드라이브 폴더에서 수동으로 정리하세요.
- 시트 데이터는 구글시트를 직접 열어서도 확인할 수 있습니다 (`projects`, `memos` 탭).
